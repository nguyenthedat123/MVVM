# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ApiAlamofire' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 5.4'
  pod 'ObjectMapper', '~> 3.1'
  platform :ios, '8.0'
  pod 'SDWebImage', '~> 5.0'
  # Pods for ApiAlamofire

end
